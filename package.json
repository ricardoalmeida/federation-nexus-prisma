{
  "name": "federation-nexus-prisma",
  "version": "1.0.0",
  "description": "",
  "main": "gateway.ts",
  "private": true,
  "scripts": {
    "start-gateway": "env-cmd ts-node-dev gateway.ts",
    "start-service-vitrola": "env-cmd ts-node-dev --transpile-only services/vitrola/src/index.ts",
    "start-services": "concurrently \"yarn:start-service-*\"",
    "postinstall": "lerna bootstrap"
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "env-cmd": "^10.1.0",
    "jest": "^26.6.1",
    "lerna": "^3.22.1",
    "ts-jest": "^26.4.3",
    "ts-node": "^9.0.0",
    "ts-node-dev": "^1.0.0",
    "typescript": "^4.0.5"
  },
  "jest": {
    "preset": "ts-jest",
    "globals": {
      "ts-jest": {
        "diagnostics": {
          "warnOnly": true
        }
      }
    },
    "testEnvironment": "node"
  },
  "dependencies": {
    "@apollo/gateway": "latest",
    "apollo-server": "latest",
    "graphql": "latest"
  }
}
