{
  "name": "shared",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "yarn -s clean && tsc"
  },
  "devDependencies": {
    "typescript": "^4.0.5"
  }
}
